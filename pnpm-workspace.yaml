packages:
  - "packages/*"
  - "apps/*"
allowBuilds:
  # Optional native addons (perf-only); skip the build and use the JS fallback — avoids node-gyp.
  '@reown/appkit': false
  bigint-buffer: false
  bufferutil: false
  esbuild: true
  keccak: false
  utf-8-validate: false
